diff --git a/compiler/third_party/npu_compiler/cmake/compiler_commit_hash.cmake b/compiler/third_party/npu_compiler/cmake/compiler_commit_hash.cmake
index 7617a6df8..07dd23749 100644
--- a/compiler/third_party/npu_compiler/cmake/compiler_commit_hash.cmake
+++ b/compiler/third_party/npu_compiler/cmake/compiler_commit_hash.cmake
@@ -3,17 +3,22 @@
 # SPDX-License-Identifier: Apache-2.0
 #
 
-execute_process(
-    COMMAND
-        git rev-parse HEAD
-    WORKING_DIRECTORY ${REPO_DIR}
-    OUTPUT_VARIABLE CURRENT_COMMIT_HASH
-    OUTPUT_STRIP_TRAILING_WHITESPACE
-    RESULT_VARIABLE ERROR_CODE
-)
+if(EXISTS "${REPO_DIR}/.git")
+    find_package(Git REQUIRED)
+    execute_process(
+        COMMAND
+            ${GIT_EXECUTABLE} rev-parse HEAD
+        WORKING_DIRECTORY ${REPO_DIR}
+        OUTPUT_VARIABLE CURRENT_COMMIT_HASH
+        OUTPUT_STRIP_TRAILING_WHITESPACE
+        RESULT_VARIABLE ERROR_CODE
+    )
 
-if (NOT ${ERROR_CODE} EQUAL 0)
-    message(FATAL_ERROR "Failed to capture compiler git commit.")
+    if(NOT ${ERROR_CODE} EQUAL 0)
+        message(FATAL_ERROR "Failed to capture compiler git commit.")
+    endif()
+else()
+    set(CURRENT_COMMIT_HASH "unknown")
 endif()
 
 set(LAST_COMMIT_HASH "")
diff --git a/compiler/third_party/npu_compiler/cmake/npu_compiler_version.cmake b/compiler/third_party/npu_compiler/cmake/npu_compiler_version.cmake
index 1db7b7f0b..80a5005cc 100644
--- a/compiler/third_party/npu_compiler/cmake/npu_compiler_version.cmake
+++ b/compiler/third_party/npu_compiler/cmake/npu_compiler_version.cmake
@@ -5,11 +5,19 @@
 
 # Function to configure NPU Compiler version properties
 function(npu_compiler_configure_version TARGET_NAME)
-    find_package(Git REQUIRED)
+    if(EXISTS "${PROJECT_SOURCE_DIR}/.git")
+        find_package(Git REQUIRED)
+    endif()
     ov_commit_hash(PLUGIN_GIT_COMMIT_HASH ${CMAKE_CURRENT_SOURCE_DIR})
+    if(NOT PLUGIN_GIT_COMMIT_HASH)
+        set(PLUGIN_GIT_COMMIT_HASH "unknown")
+    endif()
 
     # On master branch or develop branch or HEAD, VPUX_GIT_BRANCH_POSTFIX is empty, otherwise it is "-branch_name-dirty"
     ov_branch_name(VPUX_GIT_BRANCH ${CMAKE_CURRENT_SOURCE_DIR})
+    if(NOT VPUX_GIT_BRANCH)
+        set(VPUX_GIT_BRANCH "HEAD")
+    endif()
     if(NOT VPUX_GIT_BRANCH MATCHES "^(master|develop|HEAD)$")
         set(VPUX_GIT_BRANCH_POSTFIX "-${VPUX_GIT_BRANCH}-dirty")
     endif()
diff --git a/compiler/third_party/npu_compiler/cmake/prepare_cost_model_binary.cmake b/compiler/third_party/npu_compiler/cmake/prepare_cost_model_binary.cmake
index d1204fba1..67e5772cb 100644
--- a/compiler/third_party/npu_compiler/cmake/prepare_cost_model_binary.cmake
+++ b/compiler/third_party/npu_compiler/cmake/prepare_cost_model_binary.cmake
@@ -18,10 +18,12 @@ function(prepare_cost_model_binary SOURCE_FILE HEADER_FILE VARIABLE_NAME)
         message(FATAL_ERROR "File '${SOURCE_FILE}' does not exist")
     endif()
 
-    find_package(Git QUIET REQUIRED)
-    execute_process(
-        COMMAND ${GIT_EXECUTABLE} lfs pull
-        WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}/thirdparty/vpucostmodel")
+    if(EXISTS "${PROJECT_SOURCE_DIR}/thirdparty/vpucostmodel/.git")
+        find_package(Git REQUIRED)
+        execute_process(
+            COMMAND ${GIT_EXECUTABLE} lfs pull
+            WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}/thirdparty/vpucostmodel")
+    endif()
 
     file(READ ${SOURCE_FILE} hex_string HEX)
     string(LENGTH "${hex_string}" hex_string_length)
@@ -30,7 +32,10 @@ function(prepare_cost_model_binary SOURCE_FILE HEADER_FILE VARIABLE_NAME)
     math(EXPR hex_array_size "${hex_string_length} / 2")
 
     if (hex_array_size LESS "1000")
-        message(FATAL_ERROR "File '${SOURCE_FILE}' too small, check that git-lfs pull step has been done.")
+        message(FATAL_ERROR
+            "File '${SOURCE_FILE}' is a Git LFS pointer or is incomplete. "
+            "For a Git checkout, run 'git lfs pull'; for an archive, use one "
+            "that contains the complete VPUNN model files.")
     endif()
 
     set(content "
diff --git a/compiler/third_party/npu_compiler/src/vpux_driver_compiler/cmake/build_manifest.cmake b/compiler/third_party/npu_compiler/src/vpux_driver_compiler/cmake/build_manifest.cmake
index acf603647..103436466 100644
--- a/compiler/third_party/npu_compiler/src/vpux_driver_compiler/cmake/build_manifest.cmake
+++ b/compiler/third_party/npu_compiler/src/vpux_driver_compiler/cmake/build_manifest.cmake
@@ -28,6 +28,9 @@ endif()
 
 # On master branch or develop branch or HEAD, VPUX_GIT_BRANCH_POSTFIX is empty, otherwise it is "-branch_name-dirty"
 ov_branch_name(VPUX_GIT_BRANCH ${CMAKE_CURRENT_SOURCE_DIR})
+if(NOT VPUX_GIT_BRANCH)
+    set(VPUX_GIT_BRANCH "HEAD")
+endif()
 if(NOT VPUX_GIT_BRANCH MATCHES "^(master|develop|HEAD)$")
     set(VPUX_GIT_BRANCH_POSTFIX "-${VPUX_GIT_BRANCH}-dirty")
 endif()
diff --git a/compiler/third_party/npu_compiler/src/vpux_driver_compiler/src/vpux_compiler_l0/CMakeLists.txt b/compiler/third_party/npu_compiler/src/vpux_driver_compiler/src/vpux_compiler_l0/CMakeLists.txt
index 13eba879a..9f88a6b5b 100644
--- a/compiler/third_party/npu_compiler/src/vpux_driver_compiler/src/vpux_compiler_l0/CMakeLists.txt
+++ b/compiler/third_party/npu_compiler/src/vpux_driver_compiler/src/vpux_compiler_l0/CMakeLists.txt
@@ -15,10 +15,15 @@ add_library(${TARGET_NAME}
         vcl_query_network.cpp
 )
 
-find_package(Git REQUIRED)
+if(EXISTS "${PROJECT_SOURCE_DIR}/.git")
+    find_package(Git REQUIRED)
+endif()
 
 # Use commit info to create compiler version
 ov_commit_hash(PLUGIN_GIT_COMMIT_HASH ${CMAKE_CURRENT_SOURCE_DIR})
+if(NOT PLUGIN_GIT_COMMIT_HASH)
+    set(PLUGIN_GIT_COMMIT_HASH "unknown")
+endif()
 
 string(TIMESTAMP DATE_MAJOR "%m%d")
 string(TIMESTAMP DATE_MINOR "%H%M%S")
