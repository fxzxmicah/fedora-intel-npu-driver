diff --git a/compiler/third_party/npu_compiler/thirdparty/elf/npu-loader/cmake/npu_loader_version.cmake b/compiler/third_party/npu_compiler/thirdparty/elf/npu-loader/cmake/npu_loader_version.cmake
index 7c378de..b067d92 100644
--- a/compiler/third_party/npu_compiler/thirdparty/elf/npu-loader/cmake/npu_loader_version.cmake
+++ b/compiler/third_party/npu_compiler/thirdparty/elf/npu-loader/cmake/npu_loader_version.cmake
@@ -3,17 +3,22 @@
 # SPDX-License-Identifier: Apache-2.0
 #
 
-execute_process(
-    COMMAND
-        git rev-parse HEAD
-    WORKING_DIRECTORY ${CMAKE_SOURCE_DIR}
-    OUTPUT_VARIABLE CURRENT_NPU_LOADER_SHA
-    OUTPUT_STRIP_TRAILING_WHITESPACE
-    RESULT_VARIABLE ERROR_CODE
-)
+if(EXISTS "${CMAKE_SOURCE_DIR}/.git")
+    find_package(Git REQUIRED)
+    execute_process(
+        COMMAND
+            ${GIT_EXECUTABLE} rev-parse HEAD
+        WORKING_DIRECTORY ${CMAKE_SOURCE_DIR}
+        OUTPUT_VARIABLE CURRENT_NPU_LOADER_SHA
+        OUTPUT_STRIP_TRAILING_WHITESPACE
+        RESULT_VARIABLE ERROR_CODE
+    )
 
-if (NOT ${ERROR_CODE} EQUAL 0)
-    message(FATAL_ERROR "Failed to retrieve version")
+    if(NOT ${ERROR_CODE} EQUAL 0)
+        message(FATAL_ERROR "Failed to retrieve version")
+    endif()
+else()
+    set(CURRENT_NPU_LOADER_SHA "unknown")
 endif()
 
 set(LAST_NPU_LOADER_SHA "")
